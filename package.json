{
  "name": "jiangxi-travel-handbook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve -s public -l tcp://0.0.0.0:$PORT",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^4.0.0"
  }
}
